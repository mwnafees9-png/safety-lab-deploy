# Safety Lab Aero — Open Backlog & Standards Ledger
**Written 25 Jul 2026 (late)** · durable copy — the session task store reset twice tonight and ate this list twice; this file is the record. Any session picking up work: read this first, update it when you close something, commit it back.

---

## 1 · Scope decision recorded tonight (Waqas, verbatim intent)

> **"We are a 4761A tool — we don't need FMECA. For Safety Lab it will be part of the Defense Lab."**

FMECA (MIL-STD-1629A criticality: α, β, λp, t → Cm/Cr, severity–criticality matrix) is **out of scope for Safety Lab**. ARP4761A runs FMEA → FMES into the trees and never asks for 1629A criticality numbers. FMECA ships inside **Defense Lab** alongside the MIL-STD-882 pack (DEF-1, task #36), behind the defense domain-pack flag, per the PLT-1 architecture.

Battle-card consequence (improves the in-room answer): *"FMECA is scoped to our Defense Lab, where MIL-STD-1629A actually lives; the 4761A lane carries FMEA/FMES exactly as the standard asks."* Do not present FMECA as a Safety Lab gap anymore — present it as deliberate scoping.

---

## 2 · J3307 (SAE STPA standard) — CLOSED, 27/27

All 27 Table 1 work products implemented and demonstrable as of tonight's builds (waves W0–W6 + residues R1/R2), verified by the live conformance view (STPA rail step 6), INV-37 (hard link sweep), the evidence-package §10s section that re-runs the sweep at build, and 124 checks in `tests/regression_stpa.test.js`.

Morning scorecard was 3 EXISTS · 13 PARTIAL · 11 ABSENT. Closures by wave:
- **W1 spine** (1a-1, 1a-2, 1b-1, 1b-2, 1c-1, 1c-2): losses/hazards/constraints first-class, typed upward links, stakeholder confirmation on the approvals rail (kind `stpaScope`), FHA import route preserved (`fromFcId`).
- **W0/#STPA-0** (3a-1): five-part UCA with context REQUIRED at the engine (refusal posture), `seed.parts` names all five parts, legacy repair card.
- **W2** (3a-2, 3b-1, 3b-2, 3b-3, 1c-1 persistence): UCA→spine `hazardIds` (dangling refused), Step-5 constraints persisted to `acReqData` with `uca` backlink, one shared text builder, STALE marker + re-draft.
- **W3** (4a-1, 4b-1, 4b-2): six 4a causes + ten 4b causes as computed seeds per assessed UCA; coverage computed from cause-tagged scenarios; dismissal needs rationale. Also fixed latent context-drop bug in addScenario/addFactor.
- **W4** (2b-1, 2b-2, 2c-1, 2c-2, 2e-1, 2f-1, 2f-2, 2f-3): process models + four §7.2.2 mental models (humans, asked by name), positive loopSummary, responsibilities traced to constraints, `others[]` fifth element type, authority ranks with hierarchical layout, precedence rules at conflict sites, descriptions, initial/finalized with refusal.
- **W5** (§9): 27-row conformance view (rail step 6), INV-37 hard (INV-32 stays advisory by recorded decision), STPA on the golden thread (`stpa` column) + gt_integrity dangling/orphan checks + evidence package §10s.
- **W6** (informative): Appendix C archetype matrix (4 classes × 4 types, projection of W3 seeds, computed exit criterion) + optional SIP scaffold a–y (NOT REQUIRED banner, no copyrighted wording stored).
- **R1** (2a-1 residue): App E abstraction level (system/subsystem/component) declared on the Step 1 card, shown on Step 2 header, scored in 2a-1.
- **R2** (4c-1/4c-2 residue): §7.4.3.2 test-criticality disposition on every drafted requirement — critical (aligns verifMethod=Test) or non-critical with REQUIRED rationale; a bare non-critical is named as a hole in conformance evidence.

STPA lane is **opt-in via Program Planning → Program scope** (deliberate; per-program decision recorded on the plan).

---

## 3 · ARP4761A / 4754B — open gap queue (priority order)

| # | Item | Size | Notes |
|---|---|---|---|
| G-1 | ~~**G.12 failure-frequency methodology**~~ **CLOSED Sat 26 Jul** — `fta_freq.js` v1.0 shipped: w_top = Σ IB·w off the exact BDD (cofactor Birnbaum); classes initiator λ(1−q) / repairable λμ/(λ+μ) / periodic λ(1−q̄) / supplier (typed+basis) / enabler w=0 / Markov REFUSED / CCF → LOWER BOUND; N/flight = w·T; page "Frequency (G.12)"; evidence §9t/§10t (buster 2.4); tests/regression_freq.test.js 24/24; wall 63-pass + the 4 other-session suites. v1.1 (same day, user correction): frequency computes on VERIFICATION trees only (p.verifies || bottom-up) — top-down allocation trees carry probability budgets with λ stripped by design, so they get a named skip pointing at the verification mirror; 30-check suite. | — | The biggest true gap. `fta_freq.js`: w_top = Σ IB_i·w_i off the exact BDD Birnbaum importances; per-model w_i (continuous λ(1−q); latent λ(1−q̄); per-demand REFUSES without demand rate); ∫w dt per mission → per-FH; supplier-provided frequency as typed cited input; closed-form AND/OR benchmarks. |
| G-2 | ~~**§5.4.3/5.4.4 validation split**~~ **CLOSED Sat 26 Jul** — `vv_validation.js` v2.0: the §5.4.3 checklist is now AUTHORED (six aspects yes/no/n-a, notes demanded on no/n-a, lints demoted to advisory seeds, signature covers-not-replaces the answers — high rigor stays AWAITING until authored 6/6); §5.4.4 gains the authored set-level completeness judgment (signed, basis ≥15 chars, withdrawable) as check #6; INV-40 advisory (validate before you verify); PSSA gate + evidence 9e/10d carry authored/judged counts; buster 2.0; tests/regression_vv_split.test.js 31/31; wall 64-pass + the 4 other-session suites. | — |
| G-3 | **Full Markov (general CTMC)** | L | Replaces the 3-state NDF template caveat. Editor + steady-state + transient (uniformization, stated tolerance), refusals, cited benchmarks. |
| G-4 | **217F part-stress** | L | Microcircuits first (§5.1 π-factors, C1/C2), cited tables (US Gov PD), refuse-don't-default on stress inputs; parts-count stays the default lane. Cite the National Academies criticism honestly. |
| G-5 | ~~Common Resource Analysis B.4.3.2~~ | **CLOSED 25 Jul** | `cra_matrix.js` v1.0 — total-loss seeds per resource, computed consumer columns (consumedBy→traceIds→systems), authored effect/dismissal cells, IP-ledger independence findings, INV-39 advisory, evidence §9r/§10r, born-modular page (Common Resources under Common cause). 32-check suite `regression_exp_cra.test.js`. |
| G-6 | ~~G.11.1.3 named at-risk cases~~ | **CLOSED 25 Jul** | `exposure_case.js` v1.0 — four named cases with clause refs, deriveT from engine fields (NO NEW MATH), consistency contradictions (A+latent, A+partial-coverage), override demands rationale, INV-38 advisory, evidence §9q/§10q, born-modular page (Exposure cases under Trees & models). Same 32-check suite. |
| — | ~~FMECA~~ | — | **RESCOPED to Defense Lab** (see §1). |

Already closed since the 02 Jul gap analysis (verified in code): interdependence/MAC, CEA, FMES, CCMR, completion gates, combined-FC variants, monitor coverage, assumption routing, problem reports, 4754B App A objectives matrix, mod-impact, MMEL/TLD, safety-significant events, Independence Principles, qualitative dev-error events, PASA/ASA cockpits.

Honest-disclosure lines that remain until G-3/G-4 close: Markov depth; 217F parts-count-only. (G-1 frequency CLOSED 26 Jul — fta_freq.js; G-2 validation split CLOSED 26 Jul — vv_validation v2.)

---

## 4 · Recovery / coordination items (from tonight's environment resets)

1. ~~`hf_severity_badge.js` — LIVE PRODUCTION HOLE~~ **REBUILT 26 Jul (v1.1)** from the surviving call-site contract: evalRow attached to the engine export, badge + reconcile/update-effect/HFA actions, ladder imported never duplicated. 16-check suite `regression_hfw_badge.test.js` includes engine-parity (badge and INV-HFW can never disagree). Deploy heals the production 404. ✔
2. ~~`qa_hfw_badge.js`~~ — **superseded by decision**: the node parity suite covers the logic on either machine; browser-level verification happens against the live site post-deploy. Recorded, not lost. ✔
3. **`qa_stpa_kb.js`** — RESTORED from memory tonight, committed to the Mac. ✔
3b. **KB test portability** — `regression_stpa_kb` / `regression_hf_kb` now resolve the ANEM demo tree on either machine and SKIP loudly when absent (they hard-failed on the Mac before). ✔
3c. **Container declared untrustworthy for this session** — it reverted three times mid-work; the Mac tree is the working tree, walls run on the device VM (node v22 present). ✔
4. **Four suites belong to other open sessions' in-flight work** — leave alone in this lane: `regression_cert_basis_router` (wiring in ai_assistant.js), `regression_nav_ia` (index nav restructure), `regression_kestrel25` (one index registration detail), `regression_sora` (test/core mismatch — test predates sora_core's Annex-E refusal).
5. **`_restage2607/` on the Desktop repo** — recovery scratch; safe to delete.

**Session coordination rules (adopted tonight, the hard way):**
- One session writes a shared file at a time; before ANY commit, re-stage the current Desktop copy of every shared file you touched and rebase your edits onto it. Shared hot files: `index.html`, `ai_assistant.js`, `bindings_modules.js`, `helpers_modules.js`, `misc_fn_modules.js`, `data_ops_modules.js`, module registries.
- Commit to the Desktop immediately after building — container-only files do not survive environment resets.
- This ledger file is the durable task list; the in-session task widget is not.

---

## 5 · Commercial / IP / product backlog (unchanged, pre-existing)

- **IP-2 patents** (Waqas + counsel — the "patent pending" claims want filings behind them) · IP-1 minified build · IP-3 protection split
- **COM-1** commercial package (battle card DONE tonight — feeds this) · COM-2 advisor program · COM-3 TJ reviewer guide
- **DEF-1 Defense Lab** — MIL-STD-882 pack + **FMECA now lives here** (§1) · TJ Candy as reviewer
- **PRD-1** governed AI draft-tree; natural sequel: AI-proposed UCA contexts / archetype scenarios for the STPA lane (not yet logged)
- FRACAS-3 · SORA basis switch (#101/#102) · SIM-1 · Physics/Sim Lab (#46/#47) · HF-1 scoping · geom_core (CAD Lab)
- **Radia pilot** (2-month, →5 licenses): NDA inbound ~1 week (Lenny, post-Farnborough). Wants: success criteria agreed up front, onboarding path, week-8 conversion evidence. Not yet tasked.
- STPA report type in WS-B report builder (idea, not logged).

---

## 6 · Sequencing recommendation (as of tonight)

1. `hf_severity_badge.js` rebuild/re-delivery (production hole, ~1h)
2. ~~G-1 failure frequency (the DER question)~~ DONE 26 Jul
3. ~~G-2 validation split~~ DONE 26 Jul
4. Radia pilot scaffolding when the NDA lands
5. Defense Lab (DEF-1 + FMECA) when TJ engagement resumes

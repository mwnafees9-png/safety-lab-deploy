# Server-side secret vault (S8) — design & build state

**Why.** Users' AI keys and Jama API token were kept in browser localStorage — persistent, readable by any XSS. Move them onto the customer's own database, write-only from the client, server reads them to use them. Waqas ruled 14 Sep: vault BOTH Jama and AI keys; prove on local Postgres, then a throwaway Supabase.

**The model (proven).** `public.user_secrets(user_id, kind, secret, meta, updated_at)`, RLS on, and:
- The client has NO direct privilege on the table — cannot select, insert, update or delete it.
- The client writes ONLY through SECURITY DEFINER functions pinned to `auth.uid()`: `save_secret(kind, secret, meta)`, `delete_secret(kind)`.
- The client lists ONLY `my_secrets_status()` → kind + non-secret meta + updated_at. Never a value.
- `service_role` (the AI proxy, the Jama bridge) reads the value server-side.
So no client, not even the owner, can read a secret back. Portable plain-Postgres DDL (no Supabase-only extensions) → identical hosted and customer-hosted. Value is plaintext-in-column, protected by RLS-no-client-access + DB encryption at rest; pgcrypto/Vault column encryption is a later hardening.

**Proven (local Postgres 16, proof2.sql).** save via function OK; no direct table read/write (permission denied); status returns kind+meta not value; user B sees 0 of A's secrets and saving her own never touches A's; delete own OK; server (service_role) reads the live value. ALL CHECKS PASSED.

## Phases
- [x] **1. Schema + access model** — `07_user_secrets_vault.sql`, proven on local Postgres. NOT applied to any live DB yet.
- [x] **2. Real-auth proof on a throwaway Supabase** — DONE 14 Sep on the existing throwaway yiisexbngnjakkqkmctw (safety-lab-staging-stage3): migration applied, all vault checks passed with REAL authenticated/service_role roles and real auth.uid() (save/delete via functions, no raw table read/write, status hides the value, cross-user isolation, service_role reads). Test rows cleaned up.
- [x] **3. App write path** — DONE 20 Sep (safety-lab-deploy 6bfe1a1 + this commit). `site/secret_store.js` is the one place the page puts a secret; door decided from SLConfig: vault (save_secret/delete_secret/my_secrets_status; the page can never read a value back), session (browser-only: sessionStorage for the tab), desktop (keychain, unchanged). AI-key settings show saved/last4/Remove instead of echoing the value. Jama panel names its door. Legacy localStorage keys migrated once and cleared. BYO AI and Jama calls on backend doors go to the proxy with the session JWT. Regression: tests/regression_secret_store.test.js (36; runs the store in a sandbox on all three doors + static pins), mutation-tested.
- [x] **3 (as written 14 Sep)** (safety-lab-deploy) — AI-key entry (bindings_modules) and Jama connect (live_bridge) call `save_secret`/`delete_secret`/`my_secrets_status` instead of localStorage; UI shows "saved" from status. Fallbacks: browser-only door → local secure store; desktop → OS keychain (with S26).
- [x] **4. Server read path** — DONE 20 Sep (safety-lab-proxy-deploy 94cc1bd). The PROXY, not the site worker, is the one server-side reader: it tells a licence token from a Supabase JWT by shape, verifies the JWT with Supabase Auth, reads user_secrets via service_role, uses the vault key for BYO Anthropic/Voyage (never metered against SLA), and hosts /v1/ai/bridge with the Basic header built server-side and the target host pinned to the saved connector host. The site worker keeps no Supabase credential; its /api/bridge is narrowed to the browser-only door only. test/vault_auth.test.mjs (25, behavioural), mutation-tested.
- [x] **4 (as written 14 Sep)** — Jama bridge (worker.js) reads the user's `jama_token` via service_role using the caller's JWT, builds the Basic auth server-side (browser stops sending it). AI proxy (safety-lab-proxy-deploy) authenticates the user and reads their AI key from the vault for the BYO path.
- [x] **5. customer-install kit + prod** — DONE 20 Sep. Re-proven on the throwaway with real roles after the intervening migrations, then applied to production. The 17 Sep default-privileges lockdown caught this brand-new table on its own (no TRUNCATE for anon/authenticated), which is the first live proof that change works. 08_user_secrets_vault.sql already in customer-install/db and apply.sh.
- [x] **5 (as written 14 Sep)** — ship `07_user_secrets_vault.sql` in customer-install/db and apply to prod, staging-proven first.
- [x] **6. Tests** — DONE 20 Sep: proxy test/vault_auth.test.mjs (25), page tests/regression_secret_store.test.js (36), plus regression_desktop_bridge_credential updated to pin the outcome. Both deployed 21 Sep. END-TO-END WITH A REAL BROWSER SESSION NOT YET OBSERVED: user_secrets was empty at deploy; the first person to open the app with a stored key migrates it, and their next AI call proves the proxy path. Check user_secrets and audit_log after Waqas's next session.
- [x] **6 (as written 14 Sep)** across the write path, the bridge, and the proxy.

Files: `07_user_secrets_vault.sql` (the migration, portable), `stubs.sql` (Supabase-shaped stubs for the local proof: roles, auth schema, auth.uid), `proof2.sql` (the RLS/functions proof).
